const messages = {
	wrong_credentials: "Please enter correct credentials",
};

const loginMessages = {
	INVALID_CREDENTIALS: "Please enter the correct credentials",
	EMPTY_INPUT: "Please enter all the credentials to login",
};

export default messages;
